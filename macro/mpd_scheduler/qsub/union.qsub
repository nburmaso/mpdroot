#!/bin/bash
#$ -j y
#$ -N mpd-union
#$ -cwd

# request Bourne again shell as shell for job
#$ -S /bin/bash

echo "Start date: `date` (`hostname`)"

if [ "$config" != "" ]
then
 eval $config
fi

root -b -q "$VMCWORKDIR/macro/mpd_scheduler/union.C(\"$sParameters\", $UnionMode)"

echo "  End date: `date`"
