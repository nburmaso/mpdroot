#!/bin/bash
#$ -j y
#$ -N check-task
#$ -cwd
#$ -l 'hostname=nc14.jinr.ru'

# request Bourne again shell as shell for job
#$ -S /bin/bash

echo "  Start date: `date` (`hostname`)"

#if [ "$config" != "" ]
#then
# eval $config
#fi

#root -b -q "$VMCWORKDIR/macro/mpd_scheduler/union.C(\"$sParameters\")"

echo "  End date: `date`"
