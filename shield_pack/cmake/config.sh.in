export SIMPATH="${SIMPATH}"
export ROOTSYS="${ROOTSYS}"

export ROOT_LIBRARY_DIR="${ROOT_LIBRARY_DIR}"
export ROOT_INCLUDE_DIR="${ROOT_INCLUDE_DIR}"

export LD_LIBRARY_PATH="${LIBRARY_OUTPUT_PATH}:${ROOT_LIBRARY_DIR}"
export PATH="${SIMPATH}/bin:$PATH"

export TSHIELD_ROOT_PATH="${TSHIELD_ROOT_PATH}"
echo 'Variables for TShield exported.'